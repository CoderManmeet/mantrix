import type { ColorToken } from "@/constants/colors";
import type { DurationToken, GsapEaseToken, FramerEaseToken } from "@/constants/motion";
import type { RadiusToken, SpacingToken } from "@/constants/design-tokens";

/** Shared type surface for the design token system — import from here in components/hooks. */
export type {
  ColorToken,
  DurationToken,
  GsapEaseToken,
  FramerEaseToken,
  RadiusToken,
  SpacingToken,
};